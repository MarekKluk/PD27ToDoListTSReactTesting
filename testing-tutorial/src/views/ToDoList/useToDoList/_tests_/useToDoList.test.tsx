import { describe, expect, Mock, test, vi } from 'vitest';
// import { useToDoList } from "../index";
// import { renderHook } from "@testing-library/react";
// import { TestWrapper } from "../../../../../utils/testWrapper";
//
// vi.mock('../../../Api/fetchToDosApi', () => {
//   return {
//     fetchToDosApi: vi.fn()
//   };
// });
//
// describe('The UseToDoList hook', () => {
//   test('should not crash when called', () => {
//     expect(() => {
//       renderHook(() => useToDoList(), {wrapper: TestWrapper})
//     }).not.Throw;
//   })
//   // describe('The fetchToDos function', () => {
//   // 	test('should call fetchToDos with userData id', async() => {
//   // 		const fetchToDosRender = renderHook(() => useToDoList(), {wrapper: TestWrapper})
//   // // 	})
//   // })
// })
